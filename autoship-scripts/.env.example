# Example .env for autoship-scripts
# Copy this file to /opt/autoship/.env and fill in real values.
# Keep the real .env secret and set permissions to 600.

# -------------------------
# Application / API client
# -------------------------
GO_APP_PROTOCOL=http
GO_APP_HOST=localhost          # Docker container name or network alias
GO_APP_PORT=5000
GO_APP_REQUEST_TIMEOUT=10     # seconds

# -------------------------
# Host / Deployment
# -------------------------
EC2_PUBLIC_IP=your.ec2.public.ip
DNS_TTL=300

# -------------------------
# Nginx paths
# -------------------------
NGINX_SITES_AVAILABLE=/etc/nginx/conf.d
NGINX_SITES_ENABLED=/etc/nginx/conf.d

# -------------------------
# Certbot / SSL options
# -------------------------
CERTBOT_EMAIL=your-email@example.com
# Use staging=true for testing to avoid Let's Encrypt rate limits
CERTBOT_STAGING=false
CERTBOT_WEBROOT_BASE=/var/www/autoship
CERTBOT_RSA_KEY_SIZE=2048

# -------------------------
# Cloudflare (DNS)
# -------------------------
# Create a scoped API token with DNS edit permissions for the zone or use a global API key.
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ZONE_ID=your_cloudflare_zone_id_here

# -------------------------
# Logging
# -------------------------
LOG_FILE=/var/log/autoship.log

# Instructions:
# 1) Copy this file to the host path used by the service:
#    sudo cp .env.example /opt/autoship/.env
# 2) Edit /opt/autoship/.env and replace placeholders with real values.
# 3) Secure the file:
#    sudo chown root:root /opt/autoship/.env
#    sudo chmod 600 /opt/autoship/.env
# 4) Reload and restart the systemd service:
#    sudo systemctl daemon-reload && sudo systemctl restart autoship.service
